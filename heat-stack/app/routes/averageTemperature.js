/**
 * @returns The average temperature of an address based on its county_id
 */
export async function getAverageTemperatureOfAddress() {
    return 4;
}


/**
 * @param {string} address of the user
 * @returns the county_id of the county to which the address belongs
 */
export async function getCountyId(address) {
    return "x " + address;
}