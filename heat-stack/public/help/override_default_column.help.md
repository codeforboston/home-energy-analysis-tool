title: Help for the Override Default column

markdown: Check this checkbox to flip the default usage of the row’s data. For example, if the Allowed Usage icon indicates the row’s data will be used for estimating heating usage, checking this checkbox will exclude the row’s data, meaning it will NOT be used for estimating heating usage.